[project]
name = "krita-sf30"
version = "0.1.0"
description = "Python script to capture controller input and send keystrokes, like programs Antimicro or Input Remapper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "evdev>=1.9.2",
]
